export function announce(message){const el=document.getElementById('announcer');if(el){el.textContent='';requestAnimationFrame(()=>el.textContent=message)}}export function focusStep(){requestAnimationFrame(()=>document.querySelector('#step-panel h2, #step-panel h1')?.focus())}export function renderErrorSummary(errors){const box=document.getElementById('error-summary');if(!Object.keys(errors).length){box.hidden=true;box.innerHTML='';return}box.hidden=false;box.innerHTML=`<h3 tabindex="-1">Please fix the following</h3><ul>${Object.entries(errors).map(([k,v])=>`<li><a href="#${k}">${v}</a></li>`).join('')}</ul>`;box.querySelector('h3').focus()}
