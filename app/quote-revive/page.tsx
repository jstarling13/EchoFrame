import { DashboardClient } from '@/components/quote-revive/dashboard-client';

export const metadata = {
  title: 'Quote Revive — EchoFrame Intelligence',
  description:
    'Automated quote follow-up for service businesses. Stop letting quotes go cold.',
};

export default function QuoteRevivePage() {
  return <DashboardClient />;
}
